%option noyywrap
%{
	#include<stdio.h>
%}

%%
[a-zA-Z][a-zA-Z0-9_]*    { printf("Reconcido %s\n" ); }
%%
int main(void)
{
 printf("Introduce posibles identificadores (Ctrl+D para terminar):\n");
	return 0;
	yylex();
}